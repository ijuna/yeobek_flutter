name: observability
description: Logging and simple observability helpers
publish_to: "none"
version: 0.1.0
environment: { sdk: ">=3.9.0 <4.0.0" }
dependencies:
  logging: ^1.2.0
dev_dependencies:
  test: ^1.25.2