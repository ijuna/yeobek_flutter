name: features
description: Feature modules (artist, artworks, board)
publish_to: "none"
version: 0.1.0
environment: { sdk: ">=3.9.0 <4.0.0" }
dependencies:
  flutter:
    sdk: flutter
  dio: ^5.5.0
  retrofit: ^4.4.1
  json_annotation: ^4.9.0
  network:
    path: ../network
dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.25.0
  build_runner: ^2.4.12
  retrofit_generator: ^8.2.1
  json_serializable: ^6.8.0