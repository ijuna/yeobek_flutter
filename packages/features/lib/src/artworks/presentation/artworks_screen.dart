import 'package:flutter/material.dart';

class ArtworksScreen extends StatelessWidget {
  const ArtworksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Artworks')));
  }
}
